<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= poet.firstName %> <%= poet.lastName %> - Аркуші</title>
    <link href="/css/style.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body class="bg-white">

    <!-- Header -->
    <header class="bg-white border-b border-stone-200">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <div class="flex items-center gap-14">
                    <a href="/">
                        <img src="/icons/Logo.svg" alt="Аркуші" class="h-8">
                    </a>
                    <nav class="hidden md:flex items-center gap-8">
                        <a href="/poets" class="text-zinc-900 font-medium">Вірші та поети</a>
                        <a href="#" class="text-zinc-900 font-medium">Колекції</a>
                        <a href="#" class="text-zinc-900 font-medium">Про нас</a>
                    </nav>
                </div>
                <div class="hidden md:flex items-center gap-8">
                    <a href="#">
                        <img src="/icons/Icon=Telegram, State=Default.svg" alt="Telegram" class="h-4">
                    </a>
                    <a href="#">
                        <img src="/icons/Icon=Instagram, State=Default.svg" alt="Instagram" class="h-4">
                    </a>
                    <a href="#" class="text-zinc-900 font-medium">Зареєструватись</a>
                    <a href="#" class="text-red-600 font-medium border border-stone-200 rounded-md px-4 py-2 hover:bg-red-50">Задонатити на проєкт</a>
                </div>
            </div>
        </div>
    </header>

    <!-- Breadcrumbs -->
    <div class="bg-stone-50">
        <div class="container mx-auto px-4 py-5">
            <div class="flex items-center text-sm text-stone-500 font-medium">
                <a href="/" class="hover:text-stone-700">Домашня</a>
                <img src="/icons/Icon=Chevron-right, State=Default.svg" class="h-4 w-4 mx-1">
                <a href="/poets" class="hover:text-stone-700">Поети</a>
                <img src="/icons/Icon=Chevron-right, State=Default.svg" class="h-4 w-4 mx-1">
                <span><%= poet.firstName %> <%= poet.lastName %></span>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-10">
        <!-- Poet Info -->
        <section class="flex flex-col md:flex-row items-center md:items-start gap-10 mb-10">
            <img src="<%= poet.imageUrl %>" alt="<%= poet.firstName %> <%= poet.lastName %>" class="w-48 h-48 rounded-full object-cover">
            <div class="text-center md:text-left">
                <h1 class="text-4xl font-bold text-zinc-900"><%= poet.firstName %> <%= poet.lastName %></h1>
                <p class="text-lg text-stone-500 mt-1"><%= poet.birthYear %> - <%= poet.deathYear || 'сьогодення' %></p>
                <div class="prose max-w-none text-zinc-800 mt-4">
                    <p><%= poet.bio %></p>
                </div>
            </div>
        </section>

        <!-- Collections and Poems -->
        <section>
            <h2 class="text-3xl font-bold text-zinc-900 mb-6">Збірки та вірші</h2>
            <div class="space-y-8">
                <% collections.forEach(collection => { %>
                    <div>
                        <h3 class="text-xl font-semibold text-red-600 uppercase tracking-wider mb-4"><%= collection.name %></h3>
                        <ul class="space-y-2">
                            <% collection.poems.forEach(poem => { %>
                                <li>
                                    <a href="#" class="text-lg text-zinc-800 hover:text-red-600"><%= poem.title %></a>
                                </li>
                            <% }); %>
                        </ul>
                    </div>
                <% }); %>
            </div>
        </section>
    </main>

</body>
</html> 